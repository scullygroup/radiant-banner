xml.instruct! :xml, :version=>"1.0"
xml.slideshow(:shownumbers => "true", :slidedelay => "5", :transitiontime =>"1"){
  xml.banners{
    #xml.banner_id(@banner.id)
    for image in @banner.images
      xml.image(:url => url_for_file_column(image, "location", "medium"))
    end
  }
}